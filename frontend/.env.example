# API Configuration
# The base URL for the backend API
# This variable is used by all API services and SSE connections
# 
# Development: http://localhost:8080
# Production: https://your-backend-domain.com
#
# IMPORTANT: For production builds, set this as a build-time environment variable
# Example: VITE_API_URL=https://api.example.com bun run build
# Or in Docker: docker build --build-arg VITE_API_URL=https://api.example.com
VITE_API_URL=http://localhost:8080
